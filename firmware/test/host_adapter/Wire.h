#pragma once
// Platform-header adapter for <Wire.h>: the production source compiles
// unmodified and reads the injected FT6336 samples from this harness's I2C
// double.
#include "../host_hardware.h"
